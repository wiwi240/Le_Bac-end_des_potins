<!DOCTYPE html>
<html lang="fr">
<head>
  <title>The Gossip Project</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  
  <%# Cette ligne est OBLIGATOIRE pour charger ton fichier CSS %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
</head>

<body>
  <nav class="navbar navbar-expand-lg sticky-top">
    <div class="container">
      <a class="navbar-brand d-flex align-items-center" href="/">
        <%= image_tag "logo_v2.png", alt: "Logo", style: "height: 40px; width: auto; margin-right: 12px;" %>
        <span>GOSSIP</span>
      </a>

      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto align-items-center">
          <li class="nav-item"><a class="nav-link" href="/">Accueil</a></li>
          <li class="nav-item"><a class="nav-link" href="<%= gossips_path %>">Les Potins</a></li>
          <li class="nav-item"><%= link_to "Team", team_path, class: "nav-link" %></li>
          <li class="nav-item"><%= link_to "Contact", contact_path, class: "nav-link" %></li>
          
          <% if logged_in? %>
            <li class="nav-item ms-lg-3"><%= link_to "Nouveau Potin", new_gossip_path, class: "btn btn-primary text-white" %></li>
            <li class="nav-item dropdown ms-lg-3">
              <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown"><%= current_user.first_name %></a>
              <ul class="dropdown-menu dropdown-menu-end shadow border-0">
                <li><%= link_to "Profil", user_path(current_user), class: "dropdown-item" %></li>
                <li><hr class="dropdown-divider"></li>
                <li><%= button_to "Déconnexion", session_path(current_user), method: :delete, class: "dropdown-item text-danger" %></li>
              </ul>
            </li>
          <% else %>
            <li class="nav-item ms-lg-3"><%= link_to "Connexion", new_session_path, class: "nav-link" %></li>
            <li class="nav-item ms-lg-2"><%= link_to "S'inscrire", new_user_path, class: "btn btn-primary text-white" %></li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>

  <main>
    <% flash.each do |type, message| %>
      <div class="container mt-3">
        <div class="alert alert-<%= type == 'success' ? 'success' : (type == 'danger' ? 'danger' : 'info') %> shadow-sm">
          <%= message %>
        </div>
      </div>
    <% end %>

    <%= yield %>
  </main>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>